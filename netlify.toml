[build]
  base = "src"
  command = "npm install && npm run build"
  publish = "src/dist"

[[redirects]]
  from = "/api/*"
  to = "https://ai-dev-federation-dashboard.onrender.com/:splat"
  status = 200
  force = true